version: '2'
services:
   db:
      image: mysql
      ports:
        - "3306:3306"
      volumes:
        - dbvolume:/var/lib/mysql
      environment:
        - MYSQL_ROOT_PASSWORD=toor
volumes:
  dbvolume:
